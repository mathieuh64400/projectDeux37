<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>

		{% block stylesheets %}
			<link rel="stylesheet" href="{{ asset('css/styles.css') }}">
		{% endblock %}
		{# {% block javascripts %}
				        {% endblock %} #}

		<script src="{{ asset('assets/js/scripts.js') }}" defer></script>
	</head>
	{# Connectez en tant que
				{{ app.user.userIdentifier }} < #}
	<body>
		<div class="nav">
		    <a href="{{ path('app_home')}}">Home</a>
			<a href="{{ path('app_gestion') }}">Gestion</a>
			
			<a href="{{ path('app_produit')}}">Produit</a>
			{% if app.user %}
        
              <a href="{{ path('app_logout') }}">Deconnexion</a>
			  {# <a href="{{ path('app_adminE') }}">Admin</a> #}
      			 {% else %}
				<a href="{{ path('app_login')}}">Connexion</a>
    		{% endif %}
			
			{# {% if app.user.role[] is same as('ROLE_ADMIN') %}
			<a href="{{ path('app_admin') }}">Admin</a>
			{% endif %} #}
			
			

		</div>
		<div class="central">
         {% block body %}{% endblock %}
         
			</div>


			<div class="footer">
				<p>site de Mathieu 🄯 réalisé le 04/06/2023</p>
			</div>
			{# {% include "elt/_footer.html.twig" %} #}
		</body>
	</html>
